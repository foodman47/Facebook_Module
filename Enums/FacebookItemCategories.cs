namespace Facebook.Enums {
    /// <summary>
    /// 
    /// </summary>
    public enum FacebookItemCategory {
        // compatible only with page id and name abo
        Posts,
        Albums,
        Events,
        Videos,
        Photos,
        Picture
    }
}