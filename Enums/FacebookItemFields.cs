namespace Facebook.Enums {
    public enum FacebookItemFields {
        // compatible only with post, video, photo etc. id
        Shares,
        Link,
        Picture,
        Place,
        Source,
        Category,
        Fan_Count
    }
}